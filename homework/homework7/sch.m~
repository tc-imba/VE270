fid = fopen('p6.txt');
bit = fscanf(fid, '%d', 1);
n = fscanf(fid, '%d', 1);
inputs = cell(0);
if
for i=1:n
    inputs = {inputs;fscanf(fid, '%s', 1)};
end

n = fscanf(fid, '%d', 1);
outputs = fscanf(fid, '%s', n);
fclose(fid);
